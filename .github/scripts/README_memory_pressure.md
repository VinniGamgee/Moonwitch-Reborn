# Memory Pressure Manager

Implementation target: reduce allocation churn/stalls and memory-management overhead on Android/Vulkan without reducing rendering resolution. This branch is under active implementation; no UI toggle is added until the runtime path is real and validated.